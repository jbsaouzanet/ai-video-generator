// Separate Remotion entry for the sales-pitch clip (kept apart from the main Root so the films are untouched).
import {registerRoot} from 'remotion';
import {RootPitch} from './pitch/RootPitch';

registerRoot(RootPitch);
