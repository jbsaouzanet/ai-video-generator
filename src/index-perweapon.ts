// Separate Remotion entry for the Per Weapon film (kept apart from the main Root so the other cuts are untouched).
import {registerRoot} from 'remotion';
import {RootPW} from './perweapon/RootPW';

registerRoot(RootPW);
