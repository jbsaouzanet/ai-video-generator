// Separate Remotion entry for the intro (kept apart from the main Root so the video cuts are untouched).
import {registerRoot} from 'remotion';
import {RootIntro} from './intro/RootIntro';

registerRoot(RootIntro);
